{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "emitDeclarationOnly": true,
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"],
  // Browser-only UI: not part of the published package, and needs the DOM lib
  // (see tsconfig.app.json).
  "exclude": ["src/playground/app"]
}
