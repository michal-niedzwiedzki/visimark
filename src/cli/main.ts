/**
 * CLI entry point. Fully implemented in Task 10; this stub keeps the package
 * `bin` and `src/index.ts` re-export type-checkable while earlier modules land.
 */
export async function runCli(argv: string[]): Promise<number> {
  void argv;
  process.stderr.write("visimark: not yet implemented\n");
  return 2;
}
